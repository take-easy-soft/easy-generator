package <%=info.package%>;

import lombok.Data;
import javax.validation.constraints.*;

/**
 * <%=info.desc%>请求参数实体
 *
 * @author <%=info.author%>
 * @date <%=Date().toLocaleString()%>
 */
@Data
public class <%=util.firstLetter2UpperCase(info.name)%>DTO {
    <% for(const param in paramList){ %>
    /**
     * <%=paramList[param].desc%>
     */<%-util.getAnnotationByValid(paramList[param].valid,param,"\n    ",true)%>
    private <%=paramList[param].type%> <%=param%>;
    <% }; %>
}
